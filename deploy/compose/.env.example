# 本地开发数据库凭据（仅开发；生产凭据走环境变量注入，禁止提交仓库）
POSTGRES_USER=gokeep
POSTGRES_PASSWORD=gokeep_dev_password
POSTGRES_DB=gokeep
# 注意：本机若已装有 PostgreSQL 占用 5432，请改为 5433 避免端口冲突
POSTGRES_PORT=5432

REDIS_PORT=6379
REDIS_PASSWORD=
